{
	"compilerOptions": {
		/* 访问 https://aka.ms/tsconfig.json 了解更多关于此文件的信息 */

    /* 设置生成的 JavaScript 的语言版本并包含兼容的库声明。 */
		"target": "es2021",
    /* 指定一组描述目标运行时环境的捆绑库声明文件。 */
		"lib": ["es2021"],
    /* 指定生成什么 JSX 代码。 */
		"jsx": "react-jsx",

    /* 指定生成什么模块代码。 */
		"module": "es2022",
    /* 指定 TypeScript 如何从给定的模块说明符查找文件。 */
		"moduleResolution": "node",
    /* 启用导入 .json 文件 */
		"resolveJsonModule": true,

    /* 允许 JavaScript 文件成为程序的一部分。使用 `checkJS` 选项从这些文件中获取错误。 */
		"allowJs": true,
    /* 在类型检查的 JavaScript 文件中启用错误报告。 */
		"checkJs": false,

    /* 禁用从编译中生成文件。 */
		"noEmit": true,

    /* 确保每个文件都可以安全地转译而不依赖其他导入。 */
		"isolatedModules": true,
    /* 当模块没有默认导出时允许 'import x from y'。 */
		"allowSyntheticDefaultImports": true,
    /* 确保导入中的大小写正确。 */
		"forceConsistentCasingInFileNames": true,

    /* 启用所有严格类型检查选项。 */
		"strict": true,

    /* 跳过所有 .d.ts 文件的类型检查。 */
		"skipLibCheck": true,
		"types": [
			"./worker-configuration.d.ts"
		]
	}
}
